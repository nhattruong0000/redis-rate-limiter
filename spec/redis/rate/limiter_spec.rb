# frozen_string_literal: true

RSpec.describe Redis::Rate::Limiter do
  it "has a version number" do
    expect(Redis::Rate::Limiter::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
